services:
  app:
    build:
      context: .
    container_name: icloud-webview-app
    working_dir: /app
    volumes:
      - .:/app
    environment:
      PYTHONUNBUFFERED: "1"
    command: python -m app.main
    